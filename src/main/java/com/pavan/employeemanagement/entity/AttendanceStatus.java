package com.pavan.employeemanagement.entity;

/** Defines the daily attendance status for an employee. */
public enum AttendanceStatus {
    PRESENT, LATE, REMOTE, ABSENT,
    ACTIVE, INACTIVE, ON_LEAVE
}
